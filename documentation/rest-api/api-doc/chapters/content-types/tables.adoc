= Tables

Tables are two-dimensional data containers for graph entities.
They can be seen as a collection of <<Resources,resources>>, <<Literals,literals>> and <<Predicates,predicates>>.
The provided endpoints aggregate these concepts into a table representation.

NOTE: The following endpoints use content negotiation, meaning that the contents of the response json depend on the specified `Accept` and `Content-Type` headers of each request.

[[tables-fetch]]
== Fetching Tables

include::{snippets}/tables_find-by-id/description.adoc[]

operation::tables_find-by-id[snippets='path-parameters,curl-request,http-response,response-fields']

[[tables-list]]
== Listing Tables

include::{snippets}/tables_find-all/description.adoc[]

operation::tables_get-paged[snippets='curl-request']

The following list of request parameters are supported:

operation::tables_find-all[snippets='query-parameters,curl-request']

[[tables-create]]
== Creating Tables

include::{snippets}/tables_create/description.adoc[]

operation::tables_create[snippets='request-fields,curl-request,response-headers,http-response']

[[tables-edit]]
== Updating Tables

include::{snippets}/tables_update/description.adoc[]

operation::tables_update[snippets='path-parameters,request-fields,curl-request,response-headers,http-response']
