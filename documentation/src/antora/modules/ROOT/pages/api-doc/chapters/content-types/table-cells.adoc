= Table Cells

The following endpoints can be used to manipulate individual cells of tables.

NOTE: The following endpoints use content negotiation, meaning that the contents of the response json depend on the specified `Accept` and `Content-Type` headers of each request.

[[table-cells-edit]]
== Updating Table Cells

include::{snippets}/tables_update-cell/description.adoc[]

operation::tables_update-cell[snippets='path-parameters,request-fields,curl-request,response-headers,http-response,errors']
