logging:
  level:
    org.neo4j.ogm.drivers.embedded.request.EmbeddedRequest: DEBUG
orkg:
  storage:
    images:
      dir: '/tmp/orkg/images'
  init:
    setup:
      directory: 'data'
  ranking:
    papers:
      log-file: 'paper-ranking.log'
